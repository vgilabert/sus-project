using Spline;
using UnityEditor;
using UnityEngine;

namespace Editor
{
    /*[CustomEditor(typeof(SplineGenerator))]
    public class SplineGeneratorEditor : UnityEditor.Editor
    {
        public override void OnInspectorGUI()
        {
            SplineGenerator splineGenerator = (SplineGenerator) target;

            if (DrawDefaultInspector())
            {
                if (splineGenerator.autoUpdate)
                {
                    splineGenerator.DrawSplinesInEditor();
                }
            }

            if (GUILayout.Button("Generate"))
            {
                splineGenerator.DrawSplinesInEditor();
            }
        }
    }*/
}
